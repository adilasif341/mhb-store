# Migration Sources

This folder keeps non-sensitive source exports needed to rebuild the storefront catalog.

Do not commit customer exports, historical order exports, private keys, service-account JSON files, or payment/courier credentials. Put those files in `migration/private/` for local-only imports or move them into a database/admin system.
